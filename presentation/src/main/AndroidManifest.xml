<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.music.android.sensilence">

    <queries>
        <!-- Browser query
        (for "intent.resolveActivity(activity.getPackageManager()) in MusicPlayerActivity" -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.music.android.sensilence.home.MainActivity"
            android:theme="@style/AppTheme.Launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".senseofsilence.SenseOfSilenceActivity"
            android:label="@string/band_sense_of_silence"
            android:parentActivityName="com.music.android.sensilence.home.MainActivity"
            android:theme="@style/SenseOfSilenceTheme" />
        <activity android:name=".common.MusicPlayerActivity" />
    </application>

</manifest>
